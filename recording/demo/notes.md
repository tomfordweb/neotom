# Demo Project

Synthetic fixture used only to record the README demo GIF. No personal data.

- `app.ts` — drives `ts_ls`: hover, references, code action, rename.
- `util.ts` — cross-file go-to-definition / references target.
- `config.json` — extra file so Telescope find-files filtering visibly narrows.
